# Cleanup Manifest

This repository was cleaned for GitHub publication.

Removed from the public code package:

- external sample runtime copies
- pretrained or copied policy checkpoint files
- rendered demo videos
- training logs and generated output gates
- assignment notes, tables, PDF assets, screenshots, and temporary notebooks
- assignment-specific environment variable names and run names

Kept in the public code package:

- mjlab source code
- LIMX HU D03 robot model assets
- custom HU D03 velocity and tracking task integration
- clean motion generation and training tools
- empty output folders with `.gitkeep` files only

## Demo media added

- `assets/videos/locomotion-walk-0-50ms.mp4` – locomotion policy playback demo.
- `assets/videos/mimic-policy-short-run.mp4` – custom short-run mimic policy playback demo.

These videos are included for README visualization. No copied sample checkpoint weights are included.
